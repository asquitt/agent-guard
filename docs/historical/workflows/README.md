# Historical GitHub Workflows

These files were moved out of `.github/workflows/` when AgentGuard was mothballed as a standalone product on 2026-08-30. They are inert reference material and were not validated as deployable configurations by the archive change.

`SHA256SUMS` records the preserved base blobs and is enforced by `scripts/verify-mothball.sh`.

Do not move them back wholesale. A future reactivation must satisfy `PROJECT_STATUS.json`, select the minimum required workflow, repair known release and migration gaps, and complete exact-commit review before enabling any automatic trigger or cloud mutation.
